📦 User Cards Fetcher Project

Dynamically generate user profile cards using Random User API with JavaScript, and update them using a refresh button.

📚 Overview

This project fetches random user data from an API and displays them inside styled profile cards. Users can generate a new set of cards at any time by clicking the Refresh button.

It demonstrates:

Fetch API usage

Promise handling

DOM manipulation

Dynamic card creation

Responsive UI with HTML/CSS

Event handling in JavaScript

🚀 Features

✔ Fetches random user data from https://randomuser.me/api

✔ Displays user photo, name dynamically
✔ Refresh button to load new users
✔ Fully responsive and styled UI
✔ Cleaner and optimized JavaScript
✔ Modular code (easy to extend in future)
